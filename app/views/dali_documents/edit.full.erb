<script type="text/javascript">
	var dali_document_json = '<%= raw DaliDocument.find(params[:id]).json%>';
</script>
<%=render partial: 'dali_documents/dali_document'%>

<script type="text/javascript">
	var url_to_save = "/dali_documents/<%= params[:id] %>";
	dali_editor_params = {
		name: "<%= current_subject.name %>",
		id: "<%= Actor.normalize_id(current_subject) %>",
		token: "<%= form_authenticity_token %>",
		csrf_token : $("meta[name=csrf-token]").attr("content"),
        csrf_param : $("meta[name=csrf-param]").attr("content")
	};
	$(".fa-folder-open").parent().hide();
	$(".fa-save").parent().css("border-right", "2px solid #888");
	$(".navBar").css("margin-bottom", "0px");

</script>
